[project]
name = "order-matching-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
ome = "order_matching_engine:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/order_matching_engine"]

[tool.hatch.build.sources]
"src/order_matching_engine" = "order_matching_engine"
